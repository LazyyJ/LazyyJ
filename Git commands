git clone <get_the_info_from_gitlab>
git checkout -b <create_new_branch>
git push origin <name_of_the_branch>
-------------------------------------------------------------------------------------------------------------------
#Setting up gitlabs
git config --global user.name "<my_name> -- <company name>"
git config --global user.email "<your_gitlab_work_email>"
-------------------------------------------------------------------------------------------------------------------
#How to pull from specific folder and pull into CURRENT branch
#NOTE Assuming your in the current folder you want to put it in
git init --initial-branch=main
git remote add origin <add_the_specific_branch_you_wan_get_the_link_from_your_gitlab>
git add . 
git commit -m "Add comments"
---------------------------------------------------------------------------------------------------------------------
